# TeX safety

Do not use shell escape, file system access, network access, external inputs, custom binaries, or dynamic package installation.

Escape all job-derived content before placing it in TeX.
